import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const Color background = Color(0xFFF2F4F6);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF191F28);
  static const Color textSecondary = Color(0xFF8B95A1);

  // Brand
  static const Color primary = Color(0xFF3182F6);
  static const Color primaryLight = Color(0xFFEFF6FF);

  // Semantic
  static const Color success = Color(0xFF00C471);
  static const Color warning = Color(0xFFFF9F0A);
  static const Color error = Color(0xFFFF3B30);

  // Shadow
  static const Color shadowColor = Color(0x0A000000); // 4% opacity
}
